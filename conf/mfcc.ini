[DEFAULT]
sample_frequency = 16000
frame_length = 25  # the default is 25
low_freq = 20 # the default.
high_freq = 0 # the default is zero meaning use the Nyquist (4k in this case).
num_ceps = 23 # higher than the default which is 12.
snip_edges = "false"